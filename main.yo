type Date(day: int, month: int, year: int);

fn abs(x: int): int {
    val m: int;
    if x < 0 {
        ret -x;
    } else {
        ret x;
    }
}

fn mul(x: int): int {
    ret x * 2;
}

fn main(): int {
    val x: int;
    val y: int;
    val date: Date;
    val numbers: int[];

    numbers = alloc(5);
    numbers[0] = 1;
    numbers[1] = 2;
    numbers[2] = 3;
    numbers[3] = 4;
    numbers[4] = 5;

    ret numbers[3];

    date = Date::new(2, 6, 1998);

    ret date.year;

    x = 0;
    y = 1;

    while x < 7 && 2 * y < 100 {
        y = y * 2;
        x = x + 1;
    }

    ret abs(-12);
}
