//use "std/array";
use "std/string";
use "std/runtime";


fn foo(x: String): int {
    ret x.length();
}


fn main(): int {
    val x: String;
    x = "hello";
    runtime::retain(x);

    val message: String = "hello";
    runtime::retain(message);

    io::print(message);
    runtime::release(message);
    runtime::release(x);
    ret -3;
}
