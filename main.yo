use "std/string";
use "std/runtime";
use "std/array";

fn main(): int {
    val s: String = "test";
    io::print(s);
    ret s.length();
    //val array: Array = [1, 2, 3];
    //ret array.length();

}
