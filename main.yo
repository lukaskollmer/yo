type Date(year: int, month: int, day: int);

fn yearsBetween(date1: Date, date2: Date): int {
    ret date1.year - date2.year;
}


fn main(): int {
    val now:  Date;
    val then: Date;

    now = Date::new(2018, 5, 27);
    then = Date::new(1998, 6, 2);

    ret yearsBetween(now, then);
}
