// std/array.yo

use ":std/memory";


struct Array {
    count: i64,
    elements: *i64,
    allocatedSize: i64
}


impl Array {
    fn new(): Array {
        return Array::init(0, alloc<i64>(12), 12);
    }

    fn dealloc(self: Array) {
        yo_free(self.elements);
    }
}
