use ":std/intrinsics";
use ":std/operators";

use size_t = u64;
use uintptr_t = u64;

fn swap<T>(x: &T, y: &T) {
    let tmp = x;
    x = y;
    y = tmp;
}

fn fatalError(message: String) {
    printf(b"Fatal Error: %s\n", message.c_str());
    __trap();
}
